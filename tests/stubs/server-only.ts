// Stub for the "server-only" package used only during tests (see
// vitest.config.ts alias). In real Next.js builds the real package is used,
// which throws if accidentally imported from client code.
export {};
